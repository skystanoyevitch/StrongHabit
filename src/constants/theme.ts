import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#0F4D92",
    accent: "#FF2D55",
    background: "#F0F4F8", // Softer background color
    surface: "#FFFFFF",
  },
};
